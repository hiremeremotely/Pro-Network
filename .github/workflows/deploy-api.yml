# Superseded by deploy.yml — this file is intentionally inert.
# Kept to avoid git removal conflicts. Will never run automatically.
name: _disabled

on:
  workflow_dispatch:
    inputs:
      confirm:
        description: "This workflow is disabled. Use deploy.yml instead."
        required: true

jobs:
  noop:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "disabled"
