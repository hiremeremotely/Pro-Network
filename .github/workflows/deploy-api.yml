# This workflow is superseded by deploy.yml — do not use.
# It is kept as an empty file because git history requires it to be removed
# via a commit. Set the `on:` trigger to nothing so it never runs.
on: {}
